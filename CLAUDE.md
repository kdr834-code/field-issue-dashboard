# 필드이슈 대시보드 — 작업 규칙

이 저장소에서 작업할 때 반드시 따라야 하는 규칙입니다.
처음 여는 세션은 이 문서와 `docs/인수인계.md`를 먼저 읽어 주세요.

## 기술 스택 — 바꾸지 않습니다

**순수 HTML / CSS / JavaScript, 파일 하나(`index.html`)로 끝납니다.**

- React·Vue·Next.js 등 **프레임워크로 옮기자는 제안을 하지 않습니다**
- 빌드 도구(webpack·vite 등)를 도입하지 않습니다
- npm 의존성을 추가하지 않습니다 (필요한 라이브러리는 CDN에서 직접 불러옵니다)
- 파일을 여러 개로 쪼개지 않습니다

사용자가 명시한 제약입니다. 단일 파일이라 담당자가 바뀌어도 열어보면 전체가 보이고,
사내 공유 폴더에 두거나 메일로 보내도 그대로 동작하기 때문입니다.

지금 쓰는 외부 라이브러리는 **MSAL Browser v3**(로그인)과 **PDF.js**(릴리즈 PDF 읽기)
두 가지뿐이며, 차트는 라이브러리 없이 SVG로 직접 그립니다.

## 인증·데이터 구조 — 함부로 건드리지 않습니다

```
사용자 → MSAL(Azure AD) 로그인 → Microsoft Graph 토큰
      → 그 토큰으로 SharePoint 리스트 조회 (사용자 본인 권한으로)
```

**로그인은 신원 확인만 하는 게 아니라 SharePoint를 읽는 열쇠를 받아옵니다.**
로그인 방식을 다른 것(Supabase Auth 등)으로 바꾸면 Graph 토큰이 없어져
데이터 조회 자체가 불가능해집니다. 이미 검토해서 결론이 난 사항입니다
(`docs/결정기록.md` 참고).

원본은 언제나 SharePoint입니다. 데이터를 다른 곳에 복사해 두지 않습니다.

## 작업 방식

### 코드를 바꾸면 무엇을 왜 바꿨는지 한 줄로 알려줍니다

무엇을 고쳤는지보다 **왜 그렇게 했는지**가 중요합니다.

### 설명과 주석은 한국어로 씁니다

코드 주석도 한국어입니다. 그리고 "무엇을 하는 코드인지"가 아니라
**"왜 이렇게 했는지"**를 적습니다. 코드를 읽으면 알 수 있는 내용은 주석으로 쓰지 않습니다.

### 큰 수정은 미리보기로 검증한 뒤 보고합니다

`index.html`을 직접 브라우저로 열면 로그인이 필요해 확인할 수 없습니다.
가짜 데이터로 화면만 그리는 사본을 만들어 확인합니다.

```bash
node tools/makeqa.js index.html qa/index.html
node tools/serve.js qa 8093
```

`http://localhost:8093` 으로 확인합니다. 검증할 때는 다음을 봅니다.

- 문법 검사 — `index.html`의 인라인 스크립트를 뽑아 `node --check`
- `<div>` 열림/닫힘 개수 일치
- 데스크톱(1440px)·모바일(375px) 양쪽에서 8개 화면 가로 넘침 0
- 콘솔 오류 0

**스크린샷은 타이밍 때문에 자주 어긋납니다.** 화면 요소의 크기·위치는
브라우저에서 JS로 직접 재는 편이 정확합니다.

### 파일을 지우지 않습니다

삭제 대신 `trash-can/` 폴더로 옮겨만 둡니다.

### 비밀값을 다루는 방법

- `.env` 값은 **절대 화면에 출력하지 않습니다.** 필요하면 줄마다 "값 있음 / 없음"으로만 알립니다
- 외부 서비스 인증이 필요하면 **토큰 값을 사용자에게 묻지 않습니다.** `.env`에서 읽어 씁니다
- `.env`와 `node_modules`는 커밋하지 않습니다 (`.gitignore`에 등록돼 있습니다)

### 사용자 계정으로 로그인하지 않습니다

Azure AD·SharePoint·GitHub 등 사용자 계정이 필요한 작업은 **대신 수행하지 않습니다.**
무엇을 어디서 눌러야 하는지 안내하고, 사용자가 직접 하도록 합니다.

## 배포

- 호스팅은 **Netlify** 입니다 (`unique-crumble-761917.netlify.app`)
- `deploy/` 폴더는 **마지막으로 승인된 스냅샷**입니다. 자동으로 덮어쓰지 않습니다
- 배포 주소가 바뀌면 **Azure 앱 등록에 리디렉션 URI 추가**가 필요합니다
- 배포 횟수가 곧 비용입니다. 로컬 미리보기로 충분히 확인한 뒤 완성본만 올립니다

## 저장소

`index.html.backup-*` 파일은 git 이력이 대신하므로 커밋하지 않습니다.
이제 백업본을 새로 만들 필요도 없습니다 — 커밋으로 남기면 됩니다.
