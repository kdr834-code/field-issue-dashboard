# 필드이슈 대시보드 — 실행 계획 (PLAN)

작성 2026-09-17 · 품질개선팀 김동률

> [PRD.md](PRD.md)가 **무엇을 만드는가**라면, 이 문서는 **다음에 무엇부터 하는가**입니다.
> 날짜는 적지 않습니다. 담당자 한 명이 다른 업무와 병행하는 구조라 일정을 약속하면 지켜지지 않습니다.
> 대신 **순서**와 **완료 판정 기준**을 명확히 둡니다.

---

## 지금 상태

| 구분 | 상태 |
|---|---|
| 화면 8개 | 구현 완료, 로컬 미리보기로 검증 완료 |
| 배포본 | Netlify에서 운영 중 (9/14 스냅샷 기준) |
| **최신 코드** | **아직 배포되지 않음** — 최근 변경분이 운영 화면에 없음 |
| 미검증 기능 | 편집 저장 · Master Version 저장 (둘 다 실계정 필요) |

**지금 가장 중요한 사실**: 최근에 추가·수정한 기능(주간 7주, 연도별 발생기간 필터, 에러코드 추출 개선,
Master Version, 품질 도구)이 **아직 사용자에게 도달하지 않았습니다.** 코드에만 있습니다.

## 우선순위 원칙

1. **검증이 배포보다 먼저** — 저장 기능이 깨진 채 배포하면 데이터를 망칠 수 있습니다
2. **사람이 해야만 하는 일을 먼저** — 실계정 로그인이 필요한 일은 대기 시간이 생깁니다
3. **기능 추가는 마지막** — 지금 있는 것이 제대로 돌아가는지 확인이 먼저입니다

---

## 0단계 — 검증 (전부 김동률님이 직접)

실계정 로그인이 필요해 **대신 수행할 수 없는 작업**입니다. 이 단계가 끝나야 배포할 수 있습니다.

### 0-1. 편집 저장 검증 ⚠️ 위험도 높음

| | |
|---|---|
| 왜 | 한 번도 실계정으로 확인된 적이 없습니다. 특히 `분석 담당자` 사람(Person) 필드는 이름이 아니라 내부 ID로 저장해야 해서, 실패 가능성이 가장 높습니다 |
| 방법 | CLOSE 상태인 건 하나를 골라 `분석 처리결과`만 수정 → 저장 → SharePoint 원본에서 반영 확인 |
| 그다음 | 성공하면 `분석 담당자`를 목록에 이미 있는 사람으로 지정해 한 번 더 |
| 완료 판정 | 두 필드 모두 SharePoint 원본에 반영됨 |
| 실패하면 | 오류 메시지 전체를 캡처해 공유. 저장 경로를 고친 뒤 재검증 |

### 0-2. Master Version 저장 검증

| | |
|---|---|
| 왜 | PDF 파싱은 확인됐지만, SharePoint 문서 라이브러리에 저장하고 다른 사람이 읽는 경로는 미확인 |
| 방법 | 릴리즈 PDF 업로드 → ① "반영 완료" 표시 ② 문서 라이브러리에 `MasterVersion` 폴더 생성 ③ **다른 PC에서 같은 내용이 보이는지** |
| 완료 판정 | ③까지 확인됨 |
| 실패하면 | `403`이면 문서 라이브러리 쓰기 권한 문제. 새 Azure 권한은 필요 없음 (`Sites.ReadWrite.All`이 문서 드라이브 포함) |

### 0-3. Azure 앱 등록 소유자 확인

| | |
|---|---|
| 왜 | 배포 주소가 바뀔 때마다 IT 승인이 필요한지가 여기서 갈립니다. 앞으로의 호스팅 선택에 영향 |
| 방법 | Azure Portal → Microsoft Entra ID → 앱 등록 → 해당 앱 → 소유자 / 인증(Authentication) |
| 완료 판정 | 본인·팀 소유인지, IT 소유인지 확인됨 |
| 참고 | API 권한은 이미 관리자 동의 완료. 리디렉션 URI 추가는 권한 동의와 별개 설정 |

### 0-4. `error_codes.json` 공개 노출 판단

| | |
|---|---|
| 왜 | 13개 모델 463건의 서비스 매뉴얼 에러코드가 Netlify·GitHub 양쪽에서 공개 상태입니다 |
| 방법 | 회사 정보보안 기준상 문제 없는지 확인 |
| 완료 판정 | 그대로 둘지, 비공개로 돌릴지 결정됨 |
| 문제가 있으면 | GitHub 저장소를 Private으로 전환. Netlify 쪽은 별도 대응 필요 |

---

## 1단계 — 배포

0단계가 끝난 뒤에 진행합니다.

### 1-1. 최신본 배포

- `deploy/` 폴더를 현재 `index.html` + `error_codes.json` + 아이콘으로 갱신
- Netlify에 배포
- **배포 후 확인**: 로그인 → 상세 데이터 표시 → 8개 메뉴 전환 → 에러코드 검색 → 모바일 접속

### 1-2. 배포 후 사용자 안내

새로 생긴 기능을 쓰는 사람이 모르면 없는 것과 같습니다.

- 주간 분석 · 반복 불량 · Master Version · 품질 도구 4개 메뉴 신설
- 연도별 분석에 발생기간 선택 추가 (**기본값이 3개월이내로 바뀐 점**은 꼭 알려야 함 — 숫자가 달라 보입니다)
- 이미 만들어 둔 발표자료(12장)를 그대로 쓰면 됩니다

---

## 2단계 — 기능 추가 (우선순위 순)

0·1단계가 끝난 뒤 하나씩 진행합니다. 한 번에 하나만 합니다.

### 2-1. 파레토 차트 — 작업 규모 작음

상위 몇 개 원인이 전체의 몇 %를 차지하는지 보여줍니다.
"무엇부터 손대야 하는가"라는 이 대시보드의 원래 질문에 가장 직접적으로 답하는 기능입니다.

- 위치: 현황 분석 화면
- 기준: 불량유형(Lv.1) 또는 H/W 원인유형(Lv.2)
- 검증: 누적 비율이 100%로 끝나는지, 항목이 적을 때도 깨지지 않는지

### 2-2. 미입력 채우기 필터 — 작업 규모 작음

Lv.2가 비어 있는 건만 모아 보여줍니다.
반복 불량 화면에서 "Lv.2 미입력 제외" 건수를 보여주고 있는데, 정작 그 건들로 바로 갈 방법이 없습니다.

- 위치: 상세 데이터 필터에 "미입력만" 옵션
- 검증: 반복 불량 화면의 제외 건수와 이 필터의 결과 건수가 일치하는지

### 2-3. Master Version 제개정 이력 — 작업 규모 보통

릴리즈 PDF의 이력 시트도 읽어 "무엇이 언제 바뀌었는지"를 보여줍니다.

- 선행 조건: **0-2 검증 완료**
- 주의: PDF 양식이 바뀌면 파싱이 깨집니다. 실패 시 이력만 비우고 버전 표는 유지

### 2-4. FMEA / RPN 계산 — 작업 규모 보통

심각도 × 발생도 × 검출도로 위험 우선순위를 냅니다.

- 위치: 품질 도구 5번째 탭
- 주의: 다른 도구와 마찬가지로 **계산이 아니라 판정**이어야 합니다. RPN 값만 내주면 의미가 없고, 어느 구간부터 조치가 필요한지 기준을 함께 제시해야 합니다

---

## 보류 — 지금은 하지 않음

| 항목 | 상태 | 이유 |
|---|---|---|
| 로그인 유지 설정 (`sessionStorage` → `localStorage`) | 보류 | 로그인 빈도를 줄일 수 있지만, 공용 PC에서 로그아웃을 눌러야 하는 문제가 생깁니다. 사용자가 "그냥 두라"고 결정 |
| 패스키 → 비밀번호 로그인 전환 | 대상 아님 | 대시보드 코드로 바꿀 수 없습니다. Windows·Microsoft 계정 설정 영역 |

## 하지 않기로 확정한 것

재검토하지 마세요. 근거는 [docs/결정기록.md](docs/결정기록.md)에 있습니다.

- 엑셀·PPT 추출 기능
- 다른 로그인 방식(Supabase 등)으로 교체
- Supabase 등으로 호스팅 이전
- 장기 미처리 건수 지표
- SharePoint 댓글 조회

---

## 위험 요소

| 위험 | 영향 | 대응 |
|---|---|---|
| 편집 저장이 실제로는 실패 | 사용자가 고쳤다고 믿었는데 반영 안 됨 | **0-1을 배포 전에 반드시** |
| OQC 릴리즈 PDF 양식 변경 | Master Version 파싱 실패 | 파싱 실패 시 오류 메시지를 보여주고 기존 데이터를 유지. 모델명 사전 갱신 필요 |
| 담당자 1인 의존 | 부재 시 유지보수 중단 | 단일 HTML 유지 + 문서(PRD·CLAUDE·인수인계·결정기록)로 대응 |
| 배포 주소 변경 | 로그인 불가 | Azure 리디렉션 URI 추가 필요 (0-3에서 승인 경로 확인) |
| SharePoint 리스트 컬럼 변경 | 화면 일부 공백 | 컬럼을 표시 이름으로 찾으므로, 이름이 바뀌면 `FIELD` 매핑 수정 필요 |

## 작업 방식

새 기능은 [CLAUDE.md](CLAUDE.md)의 검증 흐름을 따릅니다.

```bash
node tools/makeqa.js index.html qa/index.html
node tools/serve.js qa 8093
```

문법 검사 · `<div>` 균형 · 데스크톱/모바일 가로 넘침 0 · 콘솔 오류 0을 확인한 뒤 보고합니다.

## 변경 이력

| 날짜 | 내용 |
|---|---|
| 2026-09-17 | 최초 작성 |
