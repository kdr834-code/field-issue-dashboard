# 필드이슈 대시보드 — 제품 요구사항 정의서 (PRD)

작성 2026-09-17 · 품질개선팀 김동률

> **이 문서의 성격**
> 제품을 만들기 전에 쓴 기획서가 아니라, **이미 운영 중인 제품의 요구사항을 사후에 정리한 문서**입니다.
> 지금 동작하는 것을 기준으로 적었고, 확정되지 않은 것은 「미해결」로 따로 표시했습니다.
> 판단 근거는 [docs/결정기록.md](docs/결정기록.md)에, 작업 현황은 [docs/인수인계.md](docs/인수인계.md)에 있습니다.

---

## 1. 배경

제이시스메디칼 DS팀·GS팀이 필드에서 올라온 불량 건을 팀즈(SharePoint) 리스트에 매일 입력합니다.
입력 항목은 충분하고 데이터도 계속 쌓입니다. **문제는 그 데이터로 아무것도 알아낼 수 없다는 점이었습니다.**

| 상황 | 결과 |
|---|---|
| 리스트 형태로 단순 확인만 가능 | 모델별 건수·주요 원인을 눈으로 세야 함 |
| 그래프·자동 집계 없음 | 추이를 볼 방법이 없음 |
| 키워드 검색 미지원 | 병원명·증상·에러코드로 찾을 수 없음 |
| 조건별 필터 부재 | 조건을 좁히려면 하나씩 확인 |

불량이 쌓이고 있다는 사실은 알지만 **무엇부터 손대야 하는지는 알 수 없는 상태**였습니다.

## 2. 목표

### 이루려는 것

1. **조회 시간 제거** — 조건을 고르면 집계가 즉시 나오게 한다
2. **반복 신호 발견** — 사람이 기억에 의존하던 판단을 숫자로 대체한다
3. **도입 장벽 제거** — 설치·계정 생성 없이 링크만으로 쓰게 한다
4. **유지보수 비용 최소화** — 담당자 한 명이 혼자 관리할 수 있는 수준을 넘지 않는다

### 목표가 아닌 것

- SharePoint를 대체하는 것 (원본은 계속 SharePoint)
- 입력 도구를 만드는 것 (입력은 기존대로 팀즈에서)
- 전사 시스템으로 확장하는 것 (품질개선팀 업무 범위)

## 3. 사용자

| 구분 | 인원 | 할 수 있는 일 |
|---|---|---|
| 조회 사용자 | 회사 계정(@jeisys.com) 보유자 | 모든 화면 조회 |
| 편집 담당자 | `EDIT_ALLOWED_USERS`에 등록된 계정 | 품질개선팀 담당 항목 수정, 릴리즈 PDF 업로드 |
| 운영·개발 | 1명 (품질개선팀 김동률) | 기능 추가, 배포 |

운영자가 한 명이라는 점이 **가장 강한 제약**입니다. 기술 선택은 전부 여기서 나옵니다.

## 4. 제약 조건

| 항목 | 제약 | 이유 |
|---|---|---|
| 구현 형태 | **단일 HTML 파일** (`index.html`) | 혼자 유지보수 가능해야 함. 열면 전체가 보이고, 공유 폴더에 두거나 메일로 보내도 동작 |
| 프레임워크 | 사용 금지 | 빌드 과정이 생기면 인수인계가 어려워짐 |
| npm 의존성 | 추가 금지 (CDN만) | 위와 같음 |
| 인증 | Azure AD(MSAL) 고정 | 로그인이 곧 SharePoint 조회 권한. 다른 방식으로 바꾸면 데이터가 안 나옴 |
| 데이터 저장 | 별도 DB 금지 | 원본 이중화를 만들지 않음 |
| Graph 권한 | `Sites.ReadWrite.All` (위임) | 이미 관리자 동의 완료. 추가 권한 요청 없음 |

## 5. 기능 요구사항

### F-1. 상세 데이터

- SharePoint 리스트 전체를 표로 조회한다. 정렬 기준은 접수 일자 최신순
- 병원명·제품명·불량증상·SAP ID를 하나의 키워드로 통합 검색한다
- 접수 월·발생기간·모델·제품군·국가·불량유형(Lv.1)·H/W 원인유형(Lv.2)·진행현황·에러코드를 다중 선택으로 거른다
- 접수 월 필터는 기본값으로 최근 연도만 선택한다. 사용자가 직접 바꾸면 그 선택을 유지한다
- 진행현황(OPEN/CLOSE/HOLD)을 색으로 구분하고, 각 행 왼쪽 띠로도 표시한다
- 모든 행에서 SharePoint 원본 항목으로 이동할 수 있다
- 편집 권한자는 품질개선팀 담당 항목을 수정하고 SharePoint에 저장한다
- 표 높이는 화면 높이에 맞춰 조정하고, 가로 스크롤바를 표 위쪽에도 둔다

### F-2. 현황 분석

- 선택 조건의 총 건수·OPEN·CLOSE·처리율을 KPI 카드로 보여준다
- 진행현황 비율, 불량유형(Lv.1), H/W 원인유형(Lv.2), 모델, 접수 에러코드별 건수를 집계한다
- KPI 카드·도넛 범례·막대를 누르면 해당 조건의 실제 건 목록이 펼쳐진다
- **발생기간 기본값은 `3개월이내`** — 납품 직후 발생 건이 품질 신호로 가장 중요하기 때문

### F-3. 연도별 분석

- 2025·2026·2027 연도 탭으로 전환한다. 탭에 해당 조건의 건수를 함께 표시한다
- **발생기간을 `3개월이내`·`6개월이내`·`12개월이내` 중 다중 선택**한다. 기본값은 `3개월이내`
  - 발생기간 값은 서로 겹치지 않는 구간이므로, "6개월까지"를 보려면 두 개를 함께 선택
- 모델별·제품군별·발생기간별 건수, 월별 접수 추이, 월별 완료 추이를 보여준다
- 꺾은선 그래프는 각 점 위에 건수 숫자를 항상 표시한다

### F-4. 주간 분석

- **최근 7주**(이번 주 ~ 6주 전) 중 한 주를 선택한다. 기본값은 지난주
  - 이번 주는 진행 중이라 숫자가 계속 바뀌므로 기본값에서 제외
- 그 주 신규 접수분의 건수·OPEN·CLOSE·발생기간 6개월이내 건수를 요약한다
- 전주 대비 증감을 함께 표시한다
- 모델별·불량유형별·발생기간별·국가별 집계와 그 주 접수 목록을 보여준다

### F-5. 반복 불량 감지

품질개선팀이 **지금 주의 깊게 봐야 할 신호**를 찾는 화면입니다. 처리량 지표가 아닙니다.

감지 조건 — 아래를 모두 만족할 때만 표시:

1. 진행현황이 **OPEN**
2. 발생기간이 **3개월이내 또는 6개월이내**
3. **같은 모델 · 같은 제품군 · 같은 H/W 원인유형(Lv.2)**
4. **2건 이상** (사용자가 3건 이상으로 변경 가능)

- 묶음마다 실제 접수 건 목록과 증상 문구 목록을 펼쳐볼 수 있다
- **Lv.2가 비어 있어 판정하지 못한 건수를 함께 표시한다**
  — 이 숫자가 없으면 "반복이 없다"와 "아직 원인을 안 적었다"를 구분할 수 없음

### F-6. 모델별 에러코드

- 13개 모델 463건의 에러코드를 모델 탭으로 나눠 검색한다
- 코드 접두사별로 색을 구분한다 (E=적, N=청, W=주황)
- 불량 증상 문구에서 에러코드를 자동 추출해 상세 표와 집계에 반영한다
  - 코드 뒤에 한글 부품어가 붙고 **동시에** 해당 모델 코드북에 없으면 부품 규격명으로 보고 제외
  - 코드북에 없는 코드는 **지우지 않고 옅게 표시**한다 (코드북 보완 대상 신호)

### F-7. Master Version (양산)

- OQC가 배포한 릴리즈 PDF를 업로드하면 모델별 양산 버전을 자동으로 읽어 표로 정리한다
- IPL·RF·HIFU·LASER 대분류를 접고 펼칠 수 있다. 접힘 상태를 기억한다
- 같은 모델의 연속 행은 모델명을 세로 병합하고, 향지(국내·일본·해외·중국)를 색으로 구분한다
- 모델명·향지로 검색한다
- 결과를 SharePoint 문서 라이브러리에 저장해 **모든 사용자가 같은 최신본을 본다**
- **업로드는 지정된 담당자만** — 여러 명이 올리면 최신본을 판별할 수 없음

### F-8. 품질 도구

계산기가 아니라 **판정기**입니다. 값을 넣으면 기준 충족 여부까지 답합니다.

| 도구 | 판정 내용 |
|---|---|
| 편차·공차 | 규격 안에 드는지, 한계 사용률이 얼마인지. 여러 값 일괄 판정 지원 |
| 공정능력 | Cp·Cpk — 산포 문제인지 치우침 문제인지 |
| 불량률·시그마 | PPM·시그마 수준, 표본이 판단하기에 충분한지 |
| 생산성 UPH | 개선 전후 비교. 인·초 기준으로 실제 효율 개선인지 |

## 6. 비기능 요구사항

| 항목 | 요구 |
|---|---|
| 인증 | 회사 계정만 로그인 가능. 외부 계정 차단 |
| 권한 | SharePoint에서 볼 수 있는 범위만 노출. 별도 권한 부여 없음 |
| 데이터 | 원본 복사본을 만들지 않음. 자동 새로고침 5분 주기 |
| 반응형 | PC(1440px)·모바일(375px) 모두 가로 넘침 없이 동작 |
| 접근성 | 색만으로 상태를 구분하지 않음 (배지에 항상 글자 병기) |
| 브라우저 | 크롬·엣지 기준. iOS 홈 화면 추가 지원 |

## 7. 성공 기준

지금 확인된 수치입니다.

| 지표 | 값 |
|---|---|
| 분석 화면 | **8개** |
| 자동 대조 에러코드 | **463건** (13개 모델) |
| 릴리즈 PDF 인식 정확도 | **98.3%** |
| 설치 프로그램 | **0개** |
| 신규 서버·DB | **0개** |
| 데이터 반영 지연 | 수 분 이내 (SharePoint 입력 → 화면) |

## 8. 범위 밖 — 하지 않기로 한 것

| 항목 | 이유 |
|---|---|
| 엑셀·PPT 추출 | 화면과 다른 모양이 되어 "보는 대로 추출"이 성립하지 않음. 샘플 검토 후 취소 |
| 다른 로그인 방식(Supabase 등)으로 교체 | Graph 토큰이 없어져 데이터 조회 불가 |
| Supabase 등으로 호스팅 이전 | 정적 호스팅 제품이 아님 |
| 장기 미처리 건수 지표 | 부품 수급·고객 일정 등 외부 요인이 많아 오해를 부름 |
| SharePoint 댓글 조회 | Graph API로 불가능. 원본 링크로 대체 |

## 9. 미해결 — 검증이 필요한 항목

우선순위 순입니다. 상세 검증 방법은 [docs/인수인계.md](docs/인수인계.md)에 있습니다.

1. **편집 저장이 실제로 SharePoint에 반영되는가** — 특히 `분석 담당자` 사람(Person) 필드
2. **Master Version 저장/불러오기 경로** — 문서 라이브러리 쓰기 권한
3. **Azure 앱 등록 소유자** — 배포 주소 변경 시 IT 승인 필요 여부를 결정
4. **`error_codes.json` 공개 노출** — 회사 기준 적합성

## 10. 다음 단계 후보

- 파레토 차트 (상위 20% 원인이 차지하는 비중)
- FMEA / RPN 계산 — 품질 도구에 추가
- 미입력 채우기 필터 — Lv.2가 비어 있는 건만 모아 보기
- Master Version 제개정 이력 표시

## 변경 이력

| 날짜 | 내용 |
|---|---|
| 2026-09-17 | 최초 작성 (운영 중인 제품 기준 사후 정리) |
